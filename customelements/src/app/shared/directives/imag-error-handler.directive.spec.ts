import { ImagErrorHandlerDirective } from './imag-error-handler.directive';

describe('ImagErrorHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagErrorHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
